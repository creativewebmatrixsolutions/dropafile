API Reference
=============

While `dropafile` is not designed as a library, you might want to use
single components for building bigger and better webservices. For
instance you can use the :class:`dropafile.DropAFileApplication` as WSGI app
in other environments.

.. automodule:: dropafile
   :members:
   :undoc-members:
